{
  "name": "test",
  "version": "1.0.0",
  "type": "module",
  "description": "Test suite for forms42core",
  "scripts": {
    "test": "cd dist && ls",
    "server": "live-server --port=5500 --open=./dist/index.html &",
    "build": "webpack --mode production && cp index.html styles.css dist && cp simplestyles.css dist && cp -R images dist "
  },
  "keywords": [],
  "author": "Alex Høffner",
  "license": "ISC",
  "dependencies": {
    "forms42core": "file:../core/dist"
  },
  "devDependencies": {
    "html-loader": "^3.1.0",
    "live-server": "^1.2.2",
    "ts-loader": "^9.2.7",
    "typescript": "^4.6.2",
    "webpack": "^5.70.0",
    "webpack-cli": "^4.9.2"
  }
}
